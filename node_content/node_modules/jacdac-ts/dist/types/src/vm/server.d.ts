import { PackedValues } from "../jdom/pack";
import { atomic } from "./utils";
import SensorServer from "../servers/sensorserver";
export declare const VM_EXTERNAL_REQUEST = "vmExternalRequest";
export declare class VMServiceServer extends SensorServer<any[]> {
    role: string;
    private spec;
    private eventNameToId;
    private regNameToId;
    private regFieldToId;
    private commandPackets;
    private cmdFieldToId;
    constructor(role: string, spec: jdspec.ServiceSpec);
    raiseGetRegisterEvent(regName: string): void;
    respondToGetRegisterEvent(regName: string): Promise<void>;
    sendEventNameAsync(eventName: string, values?: PackedValues): Promise<void>;
    lookupRegister(root: string, fld: string): any;
    writeRegister(root: string, ev: atomic[]): void;
}
//# sourceMappingURL=server.d.ts.map