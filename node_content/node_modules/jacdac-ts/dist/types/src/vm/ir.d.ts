import jsep from "jsep";
export interface VMError {
    sourceId?: string;
    code?: number;
    message: string;
}
export interface VMBase {
    type: "ite" | "cmd";
    sourceId?: string;
}
export interface VMIfThenElse extends VMBase {
    type: "ite";
    expr: jsep.Expression;
    then?: VMBase[];
    else?: VMBase[];
}
export interface VMCommand extends VMBase {
    type: "cmd";
    command: jsep.CallExpression;
}
export interface VMHandler {
    commands: VMBase[];
    roles?: string[];
    errors?: VMError[];
    meta?: boolean;
}
export interface VMRole {
    role: string;
    serviceClass: number;
}
export interface VMProgram {
    roles: VMRole[];
    serverRoles: VMRole[];
    handlers: VMHandler[];
}
export declare type VMFunctionNames = "start" | "halt" | "nop" | "label" | "jump" | "branchOnCondition" | "wait" | "awaitRegister" | "awaitChange" | "awaitEvent" | "awaitCondition" | "writeRegister" | "writeLocal" | "watch" | "log" | "roleBound" | "roleBoundExpression" | "onRoleConnected" | "onRoleDisconnected" | "raiseEvent";
declare type Context = "command" | "expression" | "either";
export interface VMFunctionDescription {
    id: string;
    args: (string | [string, any])[];
    prompt: string;
    context: Context;
}
export declare const VMFunctions: VMFunctionDescription[];
export {};
//# sourceMappingURL=ir.d.ts.map