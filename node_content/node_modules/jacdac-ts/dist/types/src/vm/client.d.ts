import { JDServiceClient } from "../jdom/serviceclient";
import JDService from "../jdom/service";
import { PackedValues } from "../jdom/pack";
import { atomic } from "./utils";
export declare class VMServiceClient extends JDServiceClient {
    private _registers;
    private _reportUpdate;
    private _events;
    constructor(service: JDService);
    registerRegister(regName: string, handler: () => void): void;
    registerEvent(eventName: string, handler: () => void): void;
    sendCommandAsync(commandName: string, values: PackedValues): Promise<void>;
    writeRegisterAsync(regName: string, values: atomic[]): Promise<void>;
    private writeRegAsync;
    private setEnabled;
    lookupRegisterAsync(root: string, fld: string, reportUpdate?: boolean): Promise<any>;
}
//# sourceMappingURL=client.d.ts.map