import jsep from "jsep";
import { VMError, VMProgram } from "./ir";
export declare function toIdentifier(id: string): jsep.Identifier;
export declare function toMemberExpression(root: string, field: string | jsep.Expression): jsep.MemberExpression;
export declare function compileProgram({ roles, serverRoles, handlers }: VMProgram): VMProgram;
export interface RoleRegister {
    role: string;
    register: string;
}
export interface RoleEvent {
    role: string;
    event: string;
}
export declare const getServiceFromRole: (info: VMProgram) => (role: string) => {
    spec: jdspec.ServiceSpec;
    client: boolean;
};
export declare function checkProgram(prog: VMProgram): {
    registers: RoleRegister[];
    events: RoleEvent[];
    errors: VMError[];
};
//# sourceMappingURL=compile.d.ts.map