import { JDClient } from "../jdom/client";
import { AnalogSensorServer } from "./analogsensorserver";
export declare class LevelDetector extends JDClient {
    readonly service: AnalogSensorServer;
    private _state;
    constructor(service: AnalogSensorServer);
    reset(): void;
    update(): void;
    private setState;
}
//# sourceMappingURL=leveldetector.d.ts.map