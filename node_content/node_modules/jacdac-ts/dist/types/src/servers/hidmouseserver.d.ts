import { HidMouseButton } from "../jdom/constants";
import { JDServiceServer, JDServerOptions } from "../jdom/servers/serviceserver";
/**
 * @internal
 */
export declare function renderHidMouseButtons(buttons: HidMouseButton): string;
export declare class HIDMouseServer extends JDServiceServer {
    private _lastCommand;
    constructor(options?: JDServerOptions);
    get lastCommand(): string;
    setLastCommand(s: string): void;
    private handleMove;
    private handleSetButton;
    private handleWheel;
}
//# sourceMappingURL=hidmouseserver.d.ts.map