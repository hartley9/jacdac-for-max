import { LoggerCmd, LoggerPriority } from "../jdom/constants";
import JDRegisterServer from "../jdom/servers/registerserver";
import JDServiceServer from "../jdom/servers/serviceserver";
export default class LoggerServer extends JDServiceServer {
    readonly minPriority: JDRegisterServer<[LoggerPriority]>;
    constructor();
    report(priority: LoggerCmd, msg: string): Promise<void>;
}
//# sourceMappingURL=loggerserver.d.ts.map