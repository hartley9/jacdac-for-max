import { JDRegisterServer } from "../jdom/servers/registerserver";
import { JDServiceServer } from "../jdom/servers/serviceserver";
/**
 * A server implementation of the bit:radio service
 * @category Servers
 */
export declare class BitRadioServer extends JDServiceServer {
    readonly enabled: JDRegisterServer<[boolean]>;
    readonly group: JDRegisterServer<[number]>;
    readonly transmissionPower: JDRegisterServer<[number]>;
    readonly frequencyBand: JDRegisterServer<[number]>;
    constructor();
    private handleSendString;
    private handleSendNumber;
    private handleSendValue;
    private handleSendBuffer;
    private sendRadioPacket;
}
//# sourceMappingURL=bitradioserver.d.ts.map