import JDRegisterServer from "../jdom/servers/registerserver";
import AnalogSensorServer from "./analogsensorserver";
export default class RainGaugeServer extends AnalogSensorServer {
    readonly precipitationPrecision: JDRegisterServer<[number]>;
    private _tiltCount;
    private _level;
    constructor(options?: {
        bucketSize?: number;
    });
    get tiltCount(): number;
    get level(): number;
    rain(fraction: number): Promise<void>;
    tilt(): Promise<void>;
}
//# sourceMappingURL=raingaugeserver.d.ts.map