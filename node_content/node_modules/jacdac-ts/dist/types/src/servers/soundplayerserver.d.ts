import JDRegisterServer from "../jdom/servers/registerserver";
import JDServiceServer from "../jdom/servers/serviceserver";
/**
 * @internal
 */
export declare type SoundPlayerSound = [number, string];
export default class SoundPlayerServer extends JDServiceServer {
    private readonly sounds;
    readonly volume: JDRegisterServer<[number]>;
    onPlay?: (name: string) => void;
    constructor(sounds: SoundPlayerSound[]);
    private handleListSounds;
    private handlePlay;
}
//# sourceMappingURL=soundplayerserver.d.ts.map