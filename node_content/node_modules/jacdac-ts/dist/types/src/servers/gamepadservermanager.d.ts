import JDBus from "../jdom/bus";
import { JDClient } from "../jdom/client";
export default class GamepadHostManager extends JDClient {
    readonly bus: JDBus;
    private providers;
    private ticking;
    static start(bus: JDBus): GamepadHostManager;
    constructor(bus: JDBus);
    private addEventListeners;
    private removeEventListener;
    private handleGamepadConnected;
    private handleGamepadDisconnected;
    private pollGamepads;
    private update;
    private tick;
}
//# sourceMappingURL=gamepadservermanager.d.ts.map