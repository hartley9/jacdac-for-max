import { JDServiceServer } from "../jdom/servers/serviceserver";
import { Setting } from "../jdom/setting";
export declare class SettingsServer extends JDServiceServer {
    readonly store?: Setting;
    private settings;
    constructor(store?: Setting);
    private getPayload;
    private handleGet;
    private handleSet;
    private handleDelete;
    private handleListKeys;
    private handleList;
    private handleClear;
    private read;
    private save;
}
//# sourceMappingURL=settingsserver.d.ts.map