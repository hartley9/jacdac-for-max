import { JDRegisterServer } from "../jacdac";
import { JDServerOptions, JDServiceServer } from "../jdom/servers/serviceserver";
export declare class VibrationMotorServer extends JDServiceServer {
    static VIBRATE_PATTERN: string;
    private _animation;
    private _animationStep;
    readonly maxVibrations: JDRegisterServer<[number]>;
    constructor(options?: {
        maxVibrations?: number;
    } & JDServerOptions);
    private handleRefresh;
    private handleVibrate;
}
//# sourceMappingURL=vibrationmotorserver.d.ts.map