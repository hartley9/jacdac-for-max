import JDBus from "./bus";
import { JDClient } from "./client";
/**
 * A client that bridges received and sent packets to a parent iframe.
 * @category JDOM
 */
export declare abstract class JDBridge extends JDClient {
    private _bus;
    readonly bridgeId: string;
    packetSent: number;
    packetProcessed: number;
    constructor();
    get bus(): JDBus;
    set bus(newBus: JDBus);
    /**
     * Receives frame data payload and injects it into the bus
     * @param data
     * @returns
     */
    protected receiveFrame(data: Uint8Array): void;
    /**
     * Receives packet data payload and injects it into the bus
     * @param data
     * @returns
     */
    protected receivePacket(data: Uint8Array): void;
    private dispatchPackets;
    private handleSendPacket;
    /**
     * Sends packet data over the bridge
     * @param pkt
     */
    protected abstract sendPacket(data: Uint8Array): void;
}
export default JDBridge;
//# sourceMappingURL=bridge.d.ts.map