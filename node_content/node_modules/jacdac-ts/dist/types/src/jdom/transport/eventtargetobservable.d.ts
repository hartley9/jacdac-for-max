import { Observable, Observer } from "../observable";
/**
 * @internal
 */
export declare class EventTargetObservable<TEvent> implements Observable<TEvent> {
    readonly element: EventTarget;
    readonly eventName: string;
    constructor(element: EventTarget, eventName: string);
    subscribe(observer: Observer<TEvent>): {
        unsubscribe: () => void;
    };
}
//# sourceMappingURL=eventtargetobservable.d.ts.map