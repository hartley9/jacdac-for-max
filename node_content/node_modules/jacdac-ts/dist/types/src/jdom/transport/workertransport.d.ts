import Packet from "../packet";
import { Transport, TransportOptions } from "./transport";
declare class WorkerTransport extends Transport {
    readonly type: string;
    readonly worker: Worker;
    readonly options: {
        requestDevice: () => Promise<string>;
    } & TransportOptions;
    private pending;
    constructor(type: string, worker: Worker, options: {
        requestDevice: () => Promise<string>;
    } & TransportOptions);
    private postMessageAsync;
    private handleMessage;
    protected transportSendPacketAsync(p: Packet): Promise<void>;
    protected transportConnectAsync(background?: boolean): Promise<void>;
    protected transportDisconnectAsync(background?: boolean): Promise<void>;
}
export declare function createUSBWorkerTransport(worker: Worker): WorkerTransport;
export {};
//# sourceMappingURL=workertransport.d.ts.map