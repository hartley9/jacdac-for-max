import { HF2_IO } from "./hf2";
import { Proto } from "./proto";
import { JDBus } from "../bus";
export declare class WebSerialIO implements HF2_IO {
    readonly bus: JDBus;
    private dev;
    private readLoopStarted;
    ready: boolean;
    private writer;
    private reader;
    isFreeFlowing: boolean;
    constructor(bus: JDBus);
    onData: (v: Uint8Array) => void;
    onError: (e: Error) => void;
    log(msg: string, v?: any): void;
    private clearDev;
    disconnectAsync(): Promise<void>;
    private devInfo;
    error(msg: string, code?: string): void;
    private cancelStreams;
    private readLoop;
    sendPacketAsync(pkt: Uint8Array): Promise<void>;
    private tryReconnectAsync;
    private requestDeviceAsync;
    connectAsync(background: boolean, deviceId?: string): Promise<Proto>;
    private openDeviceAsync;
}
//# sourceMappingURL=webserialio.d.ts.map