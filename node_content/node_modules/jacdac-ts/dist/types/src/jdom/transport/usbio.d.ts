/// <reference types="@types/w3c-web-usb" />
import { HF2_IO } from "./hf2";
import { Observable } from "../observable";
import { Proto } from "./proto";
export declare const USB_FILTERS: {
    filters: ({
        classCode: number;
        subclassCode: number;
        vendorId?: undefined;
        productId?: undefined;
    } | {
        vendorId: number;
        productId: number;
        classCode?: undefined;
        subclassCode?: undefined;
    })[];
};
export interface USBOptions {
    requestDevice?: (options: USBDeviceRequestOptions) => Promise<USBDevice>;
    getDevices: (options: USBDeviceRequestOptions) => Promise<USBDevice[]>;
    connectObservable?: Observable<void>;
    disconnectObservable?: Observable<void>;
}
export declare class USBIO implements HF2_IO {
    readonly options: USBOptions;
    private dev;
    private iface;
    private altIface;
    private epIn;
    private epOut;
    private readLoopStarted;
    private ready;
    private rawMode;
    constructor(options: USBOptions);
    onData: (v: Uint8Array) => void;
    onError: (e: Error) => void;
    log(msg: string, v?: any): void;
    private mkProto;
    private clearDev;
    disconnectAsync(): Promise<void>;
    recvPacketAsync(): Promise<Uint8Array>;
    private recvPacketCoreAsync;
    error(msg: string, code?: string): void;
    private readLoop;
    sendPacketAsync(pkt: Uint8Array): Promise<void>;
    private isMicrobit;
    private checkDevice;
    private tryReconnectAsync;
    private requestDeviceAsync;
    connectAsync(background: boolean, deviceId?: string): Promise<Proto>;
    private openDeviceAsync;
}
