import { Transport } from "./transport";
import { HF2_IO } from "./hf2";
import { Observable } from "../observable";
import { JDBus } from "../bus";
export declare function isWebSerialEnabled(): boolean;
export declare function isWebSerialSupported(): boolean;
export interface WebSerialOptions {
    mkTransport: (bus: JDBus) => HF2_IO;
    connectObservable?: Observable<void>;
    disconnectObservable?: Observable<void>;
}
export declare class WebSerialTransport extends Transport {
    private mkTransport;
    private hf2;
    constructor(options: WebSerialOptions);
    protected transportConnectAsync(background: boolean): Promise<void>;
    protected transportSendPacketAsync(buf: Uint8Array): Promise<void>;
    protected transportDisconnectAsync(background?: boolean): Promise<void>;
}
/**
 * Creates a transport over a Web Serial connection
 * @category Transport
 */
export declare function createWebSerialTransport(options?: WebSerialOptions): Transport;
//# sourceMappingURL=webserial.d.ts.map