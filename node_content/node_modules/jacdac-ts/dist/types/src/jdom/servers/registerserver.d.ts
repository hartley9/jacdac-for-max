import { JDServiceServer } from "./serviceserver";
import { PackedValues } from "../pack";
import { Packet } from "../packet";
import { JDEventSource } from "../eventsource";
/**
 * A register "server" instance, used to assembly service servers.
 * @category Servers
 */
export declare class JDRegisterServer<TValues extends PackedValues> extends JDEventSource {
    readonly service: JDServiceServer;
    readonly identifier: number;
    data: Uint8Array;
    lastSetTime: number;
    private readonly resetData;
    readonly specification: jdspec.PacketInfo;
    readOnly: boolean;
    errorRegister: JDRegisterServer<TValues>;
    skipBoundaryCheck: boolean;
    skipErrorInjection: boolean;
    valueProcessor: (values: TValues) => TValues;
    constructor(service: JDServiceServer, identifier: number, defaultValue?: TValues);
    get packFormat(): string;
    values(): TValues;
    private normalize;
    private shouldNormalize;
    setValues(values: TValues, skipChangeEvent?: boolean): void;
    reset(): void;
    sendGetAsync(): Promise<void>;
    handlePacket(pkt: Packet): boolean;
}
//# sourceMappingURL=registerserver.d.ts.map