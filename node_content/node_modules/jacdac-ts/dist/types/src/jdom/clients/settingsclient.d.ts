import JDService from "../service";
import { JDServiceClient } from "../serviceclient";
/**
 * A settings service client implementation.
 * @category Clients
 */
export declare class SettingsClient extends JDServiceClient {
    constructor(service: JDService);
    clear(): Promise<void>;
    listKeys(): Promise<string[]>;
    list(): Promise<{
        key: string;
        value?: Uint8Array;
    }[]>;
    setValue(key: string, value: Uint8Array): Promise<void>;
    setStringValue(key: string, value: string): Promise<void>;
    getValue(key: string): Promise<Uint8Array>;
    getStringValue(key: string): Promise<string>;
    deleteValue(key: string): Promise<void>;
}
export default SettingsClient;
//# sourceMappingURL=settingsclient.d.ts.map