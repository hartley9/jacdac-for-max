import JDBus from "./bus";
import Packet from "./packet";
/**
 * @category Trace
 * @internal
 */
export declare type CompiledPacketFilter = (pkt: Packet) => boolean;
/**
 * @category Trace
 * @internal
 */
export interface PacketFilterProps {
    announce?: boolean;
    repeatedAnnounce?: boolean;
    resetIn?: boolean;
    minPriority?: boolean;
    requiresAck?: boolean;
    ack?: boolean;
    log?: boolean;
    productIdentifiers?: number[];
    flags?: string[];
    regGet?: boolean;
    regSet?: boolean;
    devices?: Record<string, {
        from?: boolean;
        to?: boolean;
    }>;
    selfDevice?: boolean;
    serviceClasses?: number[];
    pkts?: string[];
    before?: number;
    after?: number;
    grouping?: boolean;
    pipes?: boolean;
    port?: number;
    collapseAck?: boolean;
    collapsePipes?: boolean;
    collapseGets?: boolean;
    collapseNotImplemented?: boolean;
    errors?: boolean;
}
/**
 * @internal
 */
export interface PacketFilter {
    source: string;
    props: PacketFilterProps;
    filter: CompiledPacketFilter;
}
/**
 * Given a filter text, compiles packet filter
 * @param bus
 * @param text
 * @returns
 * @category Trace
 */
export declare function parsePacketFilter(bus: JDBus, text: string): PacketFilter;
//# sourceMappingURL=packetfilter.d.ts.map