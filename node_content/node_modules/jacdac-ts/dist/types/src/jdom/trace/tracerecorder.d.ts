import JDBus from "../bus";
import JDClient from "../client";
import Trace from "./trace";
/**
 * A recorder of packets to create traces.
 * @category Trace
 */
export declare class TraceRecorder extends JDClient {
    readonly bus: JDBus;
    maxRecordingLength: number;
    private _trace;
    private _subscription;
    constructor(bus: JDBus);
    start(): void;
    stop(): Trace;
    get recording(): boolean;
    get trace(): Trace;
    private handlePacket;
}
export default TraceRecorder;
//# sourceMappingURL=tracerecorder.d.ts.map