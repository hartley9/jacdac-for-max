import { JDService } from "../jdom/service";
import { JDServiceClient } from "../jdom/serviceclient";
export declare function isMLModelSupported(model: Uint8Array, formatRegValue: number): boolean;
export declare function getMLModelFormatName(model: Uint8Array): string;
/**
 * A client for the model runner service
 * @category Clients
 */
export declare class ModelRunnerClient extends JDServiceClient {
    constructor(service: JDService);
    isModelSupported(model: Uint8Array): Promise<boolean>;
    subscribeResults(handler: (sample: number[]) => void): () => void;
    deployModel(model: Uint8Array, onProgress?: (p: number) => void): Promise<void>;
    autoInvoke(everySamples?: number): Promise<void>;
    private getReg;
    modelStats(): Promise<TFModelStats>;
}
export interface TFModelStats {
    modelSize: number;
    arenaSize: number;
    inputShape: number[];
    outputShape: number[];
    lastError: string;
}
//# sourceMappingURL=modelrunnerclient.d.ts.map