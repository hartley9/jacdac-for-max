import { AzureIotHubHealthConnectionStatus } from "../jdom/constants";
import JDService from "../jdom/service";
import { JDServiceClient } from "../jdom/serviceclient";
export declare class AzureIoTHubHealthClient extends JDServiceClient {
    private readonly hubNameRegister;
    private readonly hubDeviceIdRegister;
    private readonly connectionStatusRegister;
    constructor(service: JDService);
    get hubName(): string;
    get hubDeviceId(): string;
    get connectionStatus(): AzureIotHubHealthConnectionStatus;
    /**
     * Sends a connect command to the hub
     */
    connect(): Promise<void>;
    /**
     * Sends a disconnect command to the hub
     */
    disconnect(): Promise<void>;
    /**
     * Sends a new connection string to the iot debice
     * @param connectionString
     */
    setConnectionString(connectionString: string): Promise<void>;
}
export default AzureIoTHubHealthClient;
//# sourceMappingURL=azureiothubhealthclient.d.ts.map