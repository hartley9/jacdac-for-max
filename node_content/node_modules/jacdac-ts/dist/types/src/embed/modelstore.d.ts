import JDEventSource from "../jdom/eventsource";
import { IFile } from "./protocol";
import { ITransport } from "./transport";
export declare abstract class ModelStore extends JDEventSource {
    abstract models(): IFile[];
    abstract inputConfigurations(): IFile[];
    abstract loadFile(model: IFile): Promise<any>;
}
export declare class HostedModelStore extends JDEventSource {
    readonly transport: ITransport;
    private _models;
    constructor(transport: ITransport);
    private handleModelList;
    models(): IFile[];
    inputConfigurations(): IFile[];
    loadFile(model: IFile): Promise<Blob>;
}
//# sourceMappingURL=modelstore.d.ts.map