import { ITransport } from "./transport";
export interface IFileStorage {
    saveText(name: string, data: string): Promise<void>;
}
export declare class BrowserFileStorage implements IFileStorage {
    saveText(name: string, data: string, mimeType?: string): Promise<void>;
    private downloadUrl;
}
export declare class HostedFileStorage implements IFileStorage {
    readonly transport: ITransport;
    constructor(transport: ITransport);
    saveText(name: string, data: string): Promise<void>;
}
//# sourceMappingURL=filestorage.d.ts.map