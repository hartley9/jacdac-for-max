"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { jacdac, ArcadeGamepadReg, ArcadeGamepadEvent, ArcadeGamepadButton, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.ArcadeGamepad = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const buttonsReg = service.register(ArcadeGamepadReg.Buttons);
    buttonsReg.on(REPORT_UPDATE, () => {
        const [rest] = buttonsReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "buttons", rest.toString()]);
    });
    const downEvent = service.event(ArcadeGamepadEvent.Down);
    downEvent.on("change", () => {
        // if you need to read the event values
        const values = downEvent.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "Event_down", values.toString()]);
    });
    const upEvent = service.event(ArcadeGamepadEvent.Up);
    upEvent.on("change", () => {
        // if you need to read the event values
        const values = upEvent.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "Event_up", values.toString()]);
    });
};
