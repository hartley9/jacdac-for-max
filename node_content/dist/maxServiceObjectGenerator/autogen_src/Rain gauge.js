"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { RainGaugeReg, RainGaugeEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.RainGauge = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const precipitationReg = service.register(RainGaugeReg.Precipitation);
    precipitationReg.on(REPORT_UPDATE, () => {
        const [precipitation] = precipitationReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "precipitation", precipitation.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const precipitationPrecisionReg = service.register(RainGaugeReg.PrecipitationPrecision);
    const [precipitationPrecision] = precipitationPrecisionReg.unpackedValue;
};
