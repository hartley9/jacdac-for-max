"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { BarcodeReaderReg, BarcodeReaderEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.BarcodeReader = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const enabledReg = service.register(BarcodeReaderReg.Enabled);
    const [enabled] = enabledReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const formatsReg = service.register(BarcodeReaderReg.Formats);
    const [format] = formatsReg.unpackedValue;
};
