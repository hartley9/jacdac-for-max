"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { LightBulbReg, LightBulbEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.LightBulb = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const brightnessReg = service.register(LightBulbReg.Brightness);
    const [brightness] = brightnessReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const dimmableReg = service.register(LightBulbReg.Dimmable);
    const [dimmable] = dimmableReg.unpackedValue;
};
