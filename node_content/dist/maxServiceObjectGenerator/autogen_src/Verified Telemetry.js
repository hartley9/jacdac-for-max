"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { VerifiedTelemetryReg, VerifiedTelemetryEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.VerifiedTelemetry = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const telemetryStatusReg = service.register(VerifiedTelemetryReg.TelemetryStatus);
    telemetryStatusReg.on(REPORT_UPDATE, () => {
        const [telemetryStatus] = telemetryStatusReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "telemetryStatus", telemetryStatus.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const telemetryStatusIntervalReg = service.register(VerifiedTelemetryReg.TelemetryStatusInterval);
    const [telemetryStatusInterval] = telemetryStatusIntervalReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const fingerprintTypeReg = service.register(VerifiedTelemetryReg.FingerprintType);
    const [fingerprintType] = fingerprintTypeReg.unpackedValue;
    // get (register to REPORT_UPDATE event to enable background refresh)
    const fingerprintTemplateReg = service.register(VerifiedTelemetryReg.FingerprintTemplate);
    fingerprintTemplateReg.on(REPORT_UPDATE, () => {
        const [confidence, template] = fingerprintTemplateReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "fingerprintTemplate", confidence.toString()]);
    });
};
