"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { BaseReg, BaseEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.Base = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const instanceNameReg = service.register(BaseReg.InstanceName);
    const [instanceName] = instanceNameReg.unpackedValue;
    // get (register to REPORT_UPDATE event to enable background refresh)
    const statusCodeReg = service.register(BaseReg.StatusCode);
    statusCodeReg.on(REPORT_UPDATE, () => {
        const [code, vendorCode] = statusCodeReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "statusCode", code.toString()]);
    });
};
