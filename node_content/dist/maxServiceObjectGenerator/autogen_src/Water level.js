"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { WaterLevelReg, WaterLevelEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.WaterLevel = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const levelReg = service.register(WaterLevelReg.Level);
    levelReg.on(REPORT_UPDATE, () => {
        const [level] = levelReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "level", level.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const levelErrorReg = service.register(WaterLevelReg.LevelError);
    levelErrorReg.on(REPORT_UPDATE, () => {
        const [levelError] = levelErrorReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "levelError", levelError.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const variantReg = service.register(WaterLevelReg.Variant);
    const [variant] = variantReg.unpackedValue;
};
