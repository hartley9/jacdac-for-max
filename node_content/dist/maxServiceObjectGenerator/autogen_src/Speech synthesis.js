"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { SpeechSynthesisReg, SpeechSynthesisEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.SpeechSynthesis = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const enabledReg = service.register(SpeechSynthesisReg.Enabled);
    const [enabled] = enabledReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const langReg = service.register(SpeechSynthesisReg.Lang);
    const [lang] = langReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const volumeReg = service.register(SpeechSynthesisReg.Volume);
    const [volume] = volumeReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const pitchReg = service.register(SpeechSynthesisReg.Pitch);
    const [pitch] = pitchReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const rateReg = service.register(SpeechSynthesisReg.Rate);
    const [rate] = rateReg.unpackedValue;
    // set
};
