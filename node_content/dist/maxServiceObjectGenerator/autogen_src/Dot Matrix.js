"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { DotMatrixReg, DotMatrixEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.DotMatrix = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const dotsReg = service.register(DotMatrixReg.Dots);
    const [dots] = dotsReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const brightnessReg = service.register(DotMatrixReg.Brightness);
    const [brightness] = brightnessReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const rowsReg = service.register(DotMatrixReg.Rows);
    const [rows] = rowsReg.unpackedValue;
    // get (register to REPORT_UPDATE event to enable background refresh)
    const columnsReg = service.register(DotMatrixReg.Columns);
    const [columns] = columnsReg.unpackedValue;
    // get (register to REPORT_UPDATE event to enable background refresh)
    const variantReg = service.register(DotMatrixReg.Variant);
    const [variant] = variantReg.unpackedValue;
};
