"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { ProtoTestReg, ProtoTestEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.ProtoTest = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const rwBoolReg = service.register(ProtoTestReg.RwBool);
    const [rwBool] = rwBoolReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const roBoolReg = service.register(ProtoTestReg.RoBool);
    roBoolReg.on(REPORT_UPDATE, () => {
        const [roBool] = roBoolReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "roBool", roBool.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const rwU32Reg = service.register(ProtoTestReg.RwU32);
    const [rwU32] = rwU32Reg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const roU32Reg = service.register(ProtoTestReg.RoU32);
    roU32Reg.on(REPORT_UPDATE, () => {
        const [roU32] = roU32Reg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "roU32", roU32.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const rwI32Reg = service.register(ProtoTestReg.RwI32);
    const [rwI32] = rwI32Reg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const roI32Reg = service.register(ProtoTestReg.RoI32);
    roI32Reg.on(REPORT_UPDATE, () => {
        const [roI32] = roI32Reg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "roI32", roI32.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const rwStringReg = service.register(ProtoTestReg.RwString);
    const [rwString] = rwStringReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const roStringReg = service.register(ProtoTestReg.RoString);
    roStringReg.on(REPORT_UPDATE, () => {
        const [roString] = roStringReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "roString", roString.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const rwBytesReg = service.register(ProtoTestReg.RwBytes);
    const [rwBytes] = rwBytesReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const roBytesReg = service.register(ProtoTestReg.RoBytes);
    roBytesReg.on(REPORT_UPDATE, () => {
        const [roBytes] = roBytesReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "roBytes", roBytes.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const rwI8U8U16I32Reg = service.register(ProtoTestReg.RwI8U8U16I32);
    const [i8, u8, u16, i32] = rwI8U8U16I32Reg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const roI8U8U16I32Reg = service.register(ProtoTestReg.RoI8U8U16I32);
    roI8U8U16I32Reg.on(REPORT_UPDATE, () => {
        const [i8, u8, u16, i32] = roI8U8U16I32Reg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "roI8U8U16I32", i8.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const rwU8StringReg = service.register(ProtoTestReg.RwU8String);
    const [u8, string] = rwU8StringReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const roU8StringReg = service.register(ProtoTestReg.RoU8String);
    roU8StringReg.on(REPORT_UPDATE, () => {
        const [u8, string] = roU8StringReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "roU8String", u8.toString()]);
    });
};
