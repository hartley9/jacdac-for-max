"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { jacdac, LightLevelReg, LightLevelEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.LightLevel = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const lightLevelReg = service.register(LightLevelReg.LightLevel);
    lightLevelReg.on(REPORT_UPDATE, () => {
        const [lightLevel] = lightLevelReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "lightLevel", lightLevel.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const lightLevelErrorReg = service.register(LightLevelReg.LightLevelError);
    lightLevelErrorReg.on(REPORT_UPDATE, () => {
        const [lightLevelError] = lightLevelErrorReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "lightLevelError", lightLevelError.toString()]);
    });
};
