"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { SolenoidReg, SolenoidEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.Solenoid = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const pulledReg = service.register(SolenoidReg.Pulled);
    const [pulled] = pulledReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const variantReg = service.register(SolenoidReg.Variant);
    const [variant] = variantReg.unpackedValue;
};
