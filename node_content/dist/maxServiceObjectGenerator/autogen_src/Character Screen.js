"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { CharacterScreenReg, CharacterScreenEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.CharacterScreen = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const messageReg = service.register(CharacterScreenReg.Message);
    const [message] = messageReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const brightnessReg = service.register(CharacterScreenReg.Brightness);
    const [brightness] = brightnessReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const variantReg = service.register(CharacterScreenReg.Variant);
    const [variant] = variantReg.unpackedValue;
    // get (register to REPORT_UPDATE event to enable background refresh)
    const textDirectionReg = service.register(CharacterScreenReg.TextDirection);
    const [textDirection] = textDirectionReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const rowsReg = service.register(CharacterScreenReg.Rows);
    const [rows] = rowsReg.unpackedValue;
    // get (register to REPORT_UPDATE event to enable background refresh)
    const columnsReg = service.register(CharacterScreenReg.Columns);
    const [columns] = columnsReg.unpackedValue;
};
