"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { AzureIotHubHealthReg, AzureIotHubHealthEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.AzureIotHubHealth = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const hubNameReg = service.register(AzureIotHubHealthReg.HubName);
    hubNameReg.on(REPORT_UPDATE, () => {
        const [hubName] = hubNameReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "hubName", hubName.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const hubDeviceIdReg = service.register(AzureIotHubHealthReg.HubDeviceId);
    hubDeviceIdReg.on(REPORT_UPDATE, () => {
        const [hubDeviceId] = hubDeviceIdReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "hubDeviceId", hubDeviceId.toString()]);
    });
    // get (register to REPORT_UPDATE event to enable background refresh)
    const connectionStatusReg = service.register(AzureIotHubHealthReg.ConnectionStatus);
    connectionStatusReg.on(REPORT_UPDATE, () => {
        const [connectionStatus] = connectionStatusReg.unpackedValue;
        maxApi.outlet([service.maxID.toString(), "connectionStatus", connectionStatus.toString()]);
    });
};
