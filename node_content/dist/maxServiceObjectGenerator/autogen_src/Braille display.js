"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
//AUTOGENERATED  by generator.ts
const { BrailleDisplayReg, BrailleDisplayEvent, REPORT_UPDATE } = require("jacdac-ts");
const maxApi = require("max-api");
exports.BrailleDisplay = function (service) {
    // get (register to REPORT_UPDATE event to enable background refresh)
    const enabledReg = service.register(BrailleDisplayReg.Enabled);
    const [enabled] = enabledReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const patternsReg = service.register(BrailleDisplayReg.Patterns);
    const [patterns] = patternsReg.unpackedValue;
    // set
    // get (register to REPORT_UPDATE event to enable background refresh)
    const lengthReg = service.register(BrailleDisplayReg.Length);
    const [length] = lengthReg.unpackedValue;
};
